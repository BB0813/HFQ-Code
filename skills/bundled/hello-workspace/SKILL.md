---
name: hello-workspace
description: Introduce HFQ Code workspace conventions and how to use project AGENTS.md.
metadata:
  openclaw:
    os: ["win32", "linux", "darwin"]
---

When the user asks how this agent works in the repo:

1. Prefer project `AGENTS.md` rules.
2. Stay inside the bound workspace path.
3. Use tools for file reads/edits instead of guessing.
4. Ask before shell commands that change the system outside the repo.

Skill base directory: `{baseDir}`
