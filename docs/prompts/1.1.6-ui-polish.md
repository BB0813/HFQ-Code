# HFQ Code · 1.1.6 UI polish（开发 train）

> 基线：**1.1.5**（F1 已 ship）  
> 本 train：**纯前端手感 + F1 接缝**，不接 F2，不改 agent-core 契约。  
> 状态：in progress · 2026-07-19

## 范围

1. **Shell chrome 密度** — ActivityBar / Header / StatusBar 更紧；surface-1/2/3 层级  
2. **Coding Profile chip 热更新** — Settings 保存后 Header 立刻显示新档案名  
3. **Mermaid** — 模块级 initialize 一次；流式阶段未闭合 fence 不跑 mermaid  
4. **Goal banner** — 优先 objective 截断  
5. **Tasks 子会话分组** — running / idle / completed / failed  
6. **空态文案** — Memory / Permissions 小修  

## 非目标

- F2（goal tree / FTS / pdf-docx / dock prefs）  
- 抬版本号 / tag（开发完成后再走发版 Agent）  
- 后端 API 变更  

## 验收

- [x] Settings 改 Coding Profile → 保存 → Header chip 同步（清空则隐藏）  
- [x] 流式长 mermaid 不卡死；结束后闭合 fence 正常出图（`streaming` prop）  
- [x] 非法 mermaid 仍降级代码块  
- [x] `/goal` banner 显示 objective 优先  
- [x] Tasks 多子会话时分组标题出现（已在未提交 diff）  
- [x] `pnpm --filter @hfq/desktop run typecheck` 绿  

## 实现笔记

- `ui-store.codingProfileName` + `setCodingProfileName`  
- Settings save / load 写 store；AppHeader 订阅 store，mount 时 seed  
- `MarkdownMessage`: 模块级 mermaid init；`streaming` 时奇数 fence 当纯文本且不跑 mermaid
